apiVersion: v1
kind: Service
metadata:
  name: k8s-demo-app
  labels:
    app: k8s-demo-app
spec:
  type: ClusterIP
  selector:
    app: k8s-demo-app
  ports:
    - name: http
      port: 80
      targetPort: 3000

